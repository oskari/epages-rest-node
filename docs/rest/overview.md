# ePages Now REST API — overview

The ePages Now API is based on RESTful principles. It supports JSON media types and UTF-8 character encoding. The API uses the standard HTTP methods: `GET`, `POST`, `PUT`, `PATCH`, and `DELETE`.

- **Endpoints and schemas** — [../raml/](../raml/) (RAML specs, JSON schemas, examples per resource)
- **Data types** — [data-types.md](data-types.md)
- **HTTP behaviour** — [request-headers.md](request-headers.md), [response-headers.md](response-headers.md), [response-codes.md](response-codes.md), [media-type.md](media-type.md)
- **Rate limits** — [api-call-limit.md](api-call-limit.md)
- **API version** — [versions.md](versions.md)
